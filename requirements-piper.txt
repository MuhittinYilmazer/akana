# Offline Piper TTS (native onnxruntime). The DEFAULT TTS is edge-tts (online, in
# core); Piper is the OFFLINE fallback. Kept OUT of core so install stays light
# and robust on platforms without a Piper/onnxruntime wheel — a missing Piper
# degrades to edge at runtime (ImportError-guarded). Enable:
#   pip install -r requirements-piper.txt
#   (or `python akana.py setup --voice piper`).
piper-tts>=1.4
